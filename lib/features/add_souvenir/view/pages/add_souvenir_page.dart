//! Affichage de la page d'ajout de souvenir

import 'package:flutter/material.dart';

class AddSouvenirsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Memories"),
      ),
    );
  }
}